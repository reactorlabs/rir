sudo: required
dist: trusty

language: cpp

os:
  - osx
  - linux

compiler:
  - clang
  - gcc

env:
  - TESTS=debug
  - TESTS=recommended

matrix:
  exclude:
    - os: osx
      env: TESTS=recommended
    - os: osx
      compiler: gcc
    - os: linux
      compiler: clang

addons:
  apt:
    packages:
      - xdg-utils
      - gfortran
      - subversion
      - make
      - r-base-dev
      - liblzma-dev
      - sed
      - binutils
      - curl
      - cmake
      - rsync
      - xorg-dev

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then . ./tools/ci/before_install-osx.sh; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then . ./tools/ci/before_install-linux.sh; fi

before_script:
  - if [[ "$TESTS" == "debug" ]]; then cmake -DCMAKE_BUILD_TYPE=debug; else cmake -DCMAKE_BUILD_TYPE=release; fi
  - make setup
  - make -j4

script:
  - ./tools/tests
  - if [[ "$TESTS" == "debug" ]]; then ./tools/gnur-make check-devel; fi
  - if [[ "$TESTS" == "recommended" ]]; then ./tools/gnur-make check-recommended; fi

cache:
  - ccache
