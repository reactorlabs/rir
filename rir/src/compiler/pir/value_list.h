#ifndef COMPILER_VALUE_LIST_H
#define COMPILER_VALUE_LIST_H

#define COMPILER_VALUES(V)                                                     \
    V(True)                                                                    \
    V(False)                                                                   \
    V(Tombstone)                                                               \
    V(Missing)                                                                 \
    V(Env)                                                                     \
    V(Nil)

#endif
